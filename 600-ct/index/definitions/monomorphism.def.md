# Monomorphism

```
        f
  -------------->          m
● --------------> ● --------------> ●
x       g         a                 b

m is an monomorphism iff
  ∀ f g ∈ hom(x,a).
    ∃ m ∈ hom(a,b).
      (m ∘ f = m ∘ g) -> f = g
```
