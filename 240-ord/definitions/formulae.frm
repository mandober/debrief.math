# Order Theory :: Formulae

Upper bounds are (many) elements of ℝ not in S:

`S ⊆ ℝ -> ∃q ∈ ℝ. ∀x ∈ S. x ≤ q`

Lower bounds are (many) elements of ℝ not in S:

`S ⊆ ℝ -> ∃d ∈ ℝ. ∀x ∈ S. x ≥ d`







Infimum:

`∃m∃l( (m ∈ P ⋀ l ∈ P) ⋀ ∀x(x ∈ S -> (m ⩽ x ⋀ l ⩽ x)) ⋀ m ⩽ l)`
